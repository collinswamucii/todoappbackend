spring.application.name=todoapp

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/todolist?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT Configuration
jwt.secret=Xtkliep+P6nOyt9vC7StkSk0lveqaWbp68t7PvgXqhavuvWHCosNhkUTv4mlVtlK
jwt.expiration=36000000

# Enable Swagger UI (already enabled by default)
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# Optional: Show only the endpoints you care about (e.g., under /api)
springdoc.paths-to-match=/api/**
